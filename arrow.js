const doubleIt=num=>num*2;
const add=(x,y)=>x+y;

const give5=()=>4;

const bishal=(x,y)=>{
    const add=x+y;
    const diff=x-y;
    const result=add*diff;
    return result;
}

const result2=bishal(40,10);
console.log(result2);